export HOMEBREW_NO_ANALYTICS=1
typeset -U path PATH

[ -s $HOME/.homebrew/bin/brew ] && eval "$($HOME/.homebrew/bin/brew shellenv)"

path=($HOME/.emacs.d/bin $HOME/.local/bin $path)

export VOLTA_HOME="$HOME/.volta"
path=($VOLTA_HOME/bin $path)

export PYENV_ROOT="$HOME/.pyenv"
path=($PYENV_ROOT/bin $path)
if type pyenv &>/dev/null
then
  eval "$(pyenv init -)"
fi

export DOTNET_CLI_TELEMETRY_OPTOUT=1
export DOTNET_ROOT="$HOME/.dotnet"
path=($DOTNET_ROOT $DOTNET_ROOT/tools $path)

[ -s $HOME/.cargo/env ] && \. $HOME/.cargo/env

if type nvim &>/dev/null
then
  export EDITOR="nvim"
else
  export EDITOR="vim"
fi

path=($HOME/Library/Application\ Support/JetBrains/Toolbox/scripts $path)

export PATH
export ZSHENV_PATH=$PATH
export ZSHENV_MANPATH=$MANPATH
